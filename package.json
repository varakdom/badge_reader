{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build --legacy-peer-deps",
    "dev": "nuxt dev --legacy-peer-deps",
    "generate": "nuxt generate --legacy-peer-deps",
    "preview": "nuxt preview --legacy-peer-deps",
    "postinstall": "nuxt prepare --legacy-peer-deps"
  },
  "dependencies": {
    "firebase": "^10.0.0",
    "mqtt": "^5.10.2",
    "nuxt": "^3.14.159",
    "nuxt-vuefire": "^1.0.4",
    "vue": "latest",
    "vue-router": "latest",
    "vuefire": "^3.2.0"
  }
}
